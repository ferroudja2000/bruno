info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: https://api.efi-academy.com/user-api/public/login
  body:
    type: json
    data: |-
      {
        "email": "jean@example.com",
        "password": "secret123"
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: bru.getCollectionVaremail(email);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
